import { Stack } from "expo-router";

const MedicineCalculatorLayout = () => {
	return (
		<Stack
			screenOptions={{
				headerShown: false,
			}}
		/>
	);
};

export default MedicineCalculatorLayout;
